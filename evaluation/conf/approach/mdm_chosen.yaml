# Cost learning anchored on the executed correction (the generator's intended
# framing): the mdm correction is the "chosen" path the LLM distills.
defaults:
  - grounder: mdm
  - cost_gen: immediate
  - steering: none
  - _self_
_target_: evaluation.approaches.base.Approach
name: mdm_chosen
