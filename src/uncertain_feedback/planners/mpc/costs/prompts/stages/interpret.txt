You are the caregiver of a person with limited arm mobility. The robot was already moving their arm toward a useful goal when they interrupted it with one spoken instruction, and the robot generated several candidate motions that could satisfy that instruction. The person picked ONE candidate and may have explicitly marked some candidates as wrong. Only marked-wrong candidates are shown; unmarked candidates carry no preference signal. The spoken instruction defines the overall correction. The candidate selection is used only to disambiguate its joint-space interpretation and preferred degree. This is the FIRST of three stages: your only job here is to say, in plain human language, what the person actually wanted — what made the motion they picked right and what the marked-wrong candidates got wrong. You are NOT writing code and NOT choosing any numbers in this stage.

Instruction:
{instruction}

{current_cluster_traj_img}
{other_clusters_traj_img}
{reference_traj_img}
In every attached image the path the person CHOSE is the full blue arm (sampled poses along its motion, no start/end markers), and the orange arm is the shared current pose every candidate starts from. The GOLD STAR is the original left-wrist task-space goal. A candidate is a short correction motion, not necessarily a complete trajectory to that goal.

When "candidate_comparison" is present in the compact summary, use it to compare terminal joint features. "chosen_end" belongs to "chosen_rollout"; "current_value" belongs to "current_rollout"; "original_plan_end" belongs to "original_plan_rollout"; and every entry in "rejected_ends" maps one statistic to the marked-wrong rollout that produced it. "chosen_minus_original_plan" shows whether that feature can distinguish the correction from the interrupted plan. "standardized_separation" is (chosen_end - rejected_median) / rejected_std, so its sign gives the chosen direction and its magnitude gives separation in marked-wrong-rollout standard deviations; it is null when the marked-wrong rollouts have no usable spread.

How to think:
- Use the spoken instruction for the overall correction. First require that the joint dimension changes the chosen motion away from the original plan/current posture in a direction consistent with that instruction. Then compare the chosen (blue) path against the marked-wrong (grey) candidates, when any exist, and "candidate_comparison" to resolve its joint-space interpretation and preferred degree. Large chosen-versus-marked-wrong separation alone is not enough when that feature is unchanged from, or moves the wrong way relative to, the original plan. What distinguishes the selection is the posture the arm holds — where it ends up and any persistent way it is held along the path.
- Write ONE plain sentence a caregiver would say out loud about what makes THIS one different — e.g. "they wanted their hand up high in front of them, not out to the side" or "they wanted the elbow to stay bent rather than reaching straight out."
- Name the single dimension that most separates the chosen path from the others, and name it as a JOINT being held differently — this is the PRIMARY local correction the next stage grounds first. Use exactly one of: elbow bend (flexion), shoulder raised forward/back (flexion/extension), arm out to the side vs. across the body (abduction/adduction), overall arm height regardless of direction (elevation), or arm rotation. The primary correction uses exactly one such dimension; only an explicit relationship spoken in the instruction may additionally name a bounded dimension and a conditioning dimension (see "relationship" below).
- Note whether the correction suggests the person is uncomfortable reaching the original goal (gold star / green reference) the way it was going, or whether the goal is still fine and they just want the motion shaped along the way.
- Do not treat incidental wiggles, exact timing, or every detail of the generated path as a user preference. However, consider persistent trajectory-level evidence such as coordination between joints, transient extrema, and avoided regions. When this structure is explicitly stated in the instruction, use it in the immediate interpretation. Otherwise, record it as associated evidence without turning it into a global preference from one interaction.
- Treat your interpretation as the simplest LOCAL explanation of this one correction at the current pose — provisional evidence for later rounds, not automatically a global mobility restriction.

Return ONLY a JSON object, no prose, with these keys:
- "preference": the one plain caregiver sentence (string).
- "distinguishing_dimension": the single most-separating JOINT, as one of exactly these five: "elbow flexion", "shoulder flexion/extension", "shoulder abduction/adduction", "shoulder elevation", "shoulder internal/external rotation".
- "direction": which way along that joint the chosen path improves on the original plan (string), e.g. "more bent", "raised more forward", "less out to the side"; if the chosen value lies between the original and marked-wrong values, describe the preferred intermediate range rather than only saying higher/lower.
- "secondary": an optional second preference only if one is clearly implied, else "" (string).
- "goal_conflict": true only if the correction implies the person is uncomfortable reaching the original goal that way; false if the goal is still reachable and they only want the motion shaped (boolean).
- "relationship": an object or null. Non-null ONLY when the SPOKEN INSTRUCTION explicitly relates two joints (e.g. "bend my elbow more as you raise my arm", "I can't bend my elbow that much while my arm is raised"). When non-null: {"bounded_dimension": the joint whose acceptable range is constrained, "conditioning_dimension": the joint its range depends on, "relationship_direction": in words, how the bound on the bounded dimension shifts as the conditioning dimension changes, "evidence": a string that MUST quote the instruction, prefixed "instruction: '...'"}. Correlated joint changes seen only in the selected trajectory do NOT license a relationship — leave it null and record them under "associated_changes".
- "associated_changes": a list (possibly empty). Each item is a persistent, meaningful secondary change in the selected correction that is neither the primary local correction nor explicitly stated: {"dimension", "direction", "temporal_scope", "evidence"}. Include only persistent structure (joint coordination, a transient extremum, an avoided region) — never incidental wiggles or timing.
- "unresolved_explanations": a list (possibly empty) of short strings naming competing explanations this one interaction cannot decide between (e.g. "constant elbow-flexion preference", "pose-dependent elbow-flexion/shoulder-elevation preference"). Carried forward for multi-round synthesis, not acted on now.
- "evidence": an object with keys "preference", "distinguishing_dimension", and "direction"; each value is ONE short string citing the strongest basis for that conclusion, prefixed by its kind: "images: ..." (which attached image and what it showed), "summary: ..." (which trajectory's feature/statistic and its value), or "instruction: '...'" (a short quote).

Rules:
- "relationship" must be non-null only when the spoken instruction explicitly relates two features; its evidence must cite the instruction, not merely trajectory correlation.
- Correlated joint changes in one selected trajectory are insufficient to establish a global relationship.
- "associated_changes" should include only persistent, meaningful changes — not incidental wiggles. Empty "associated_changes" and "unresolved_explanations" lists are valid.
- If observed structure cannot be expressed by the current cost API, preserve it as evidence rather than inventing an unsupported cost.

Compact summary:
{summaries}
