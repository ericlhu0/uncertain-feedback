You are grounding a care preference in trajectory data. This is the SECOND of three stages. You are given a plain-language preference (from stage one) and the full numeric trajectory summaries. Your only job is to turn that preference into a concrete NUMERIC specification a programmer could implement directly. You are NOT writing code in this stage.

Preference (from stage one):
{interpretation}

Available joint-angle features (all in radians, all you may specify a preference over):
- elbow_flexion: how bent the elbow is (larger = more bent; 0 = arm fully straight, π/2 = bent to a right angle, π = forearm folded back against the upper arm).
- shoulder_flexion_extension: signed depth of the upper arm (0 = neither forward nor back, +π/2 = pointing straight forward, −π/2 = straight back).
- shoulder_abduction_adduction: signed lateral position of the upper arm (0 = neither out nor across, +π/2 = straight out to the side away from the torso for the left arm, −π/2 = fully across the body).
- shoulder_elevation: total upper-arm elevation from straight down (0 = arm at side, π/2 = horizontal, π = overhead); increases as the arm is raised in any direction.
- shoulder_internal_external_rotation: signed twist of the upper arm about its long axis (0 = neutral/untwisted, positive = internal rotation twisting the forearm toward the body midline, negative = external rotation twisting it away).
Each of these appears in the summaries under "joint_features" for every trajectory, with min / median / max / start / end values.

How to ground it:
- Choose the FEWEST features that express the preference — default to exactly one. Add a second only if stage one's "secondary" clearly calls for a distinct one.
- Ground only the PRIMARY local correction, plus the explicit relationship if stage one returned one. Do NOT turn stage one's "associated_changes" or "unresolved_explanations" into additional cost terms, and do not infer a pose-dependent coupling from a correlation seen only in the selected trajectory; carry those fields forward untouched for multi-round synthesis.
- Translate the preference into ONE of these bound types per feature, never an exact target unless the preference clearly demands a precise posture:
  - "lower_bound": penalize the feature going BELOW a value (e.g. keep the elbow at least this bent).
  - "upper_bound": penalize the feature going ABOVE a value.
  - "band": penalize leaving a [low, high] comfort range.
- A lower_bound or upper_bound may be CONSTANT (a single fixed threshold) or POSE-DEPENDENT (its threshold shifts with the current value of another feature — e.g. "the more you raise my arm, the more my elbow must stay bent" couples the elbow_flexion floor to shoulder_elevation). Make a bound pose-dependent ONLY when stage one returned a non-null "relationship" (an explicitly spoken coupling between two joints): use its "bounded_dimension" as the bounded feature and its "conditioning_dimension" as the "cond_feature", read the relationship's direction from stage one, and use the selected trajectory only to ground the numeric anchors, magnitude, and direction — not to justify the coupling itself. If "relationship" is null, use a CONSTANT bound for the primary correction; a correlation visible only in the selected trajectory is NOT sufficient to introduce a coupling. A pose-dependent bound is still ONE term even though it names two features, and its shape need not be a straight line.
- Pick the numeric value(s) from the summaries. A feature value on the CHOSEN trajectory (its "mdm_traj" end or median) is an EXAMPLE inside a broader comfortable set — turn it into a bound with margin, not the exact number. State which trajectory and which statistic each value came from.
- The resulting total cost MUST score the chosen correction strictly lower than the "reference" original plan and each marked-wrong candidate. Do not choose a one-sided bound that gives these trajectories the same penalty. If the chosen value lies between the reference and marked-wrong values, use a band when that is the smallest specification that prefers the chosen motion to all of them.
- Stay goal-safe. A "reference" summary (the original-goal path the robot was taking before the correction) is usually present. Every bound you choose MUST already be satisfied by the chosen trajectory's end — check the summaries. Do NOT widen a bound to contain the reference's end: the reference end is the posture the person corrected away from, and the arm can usually reach the same goal position in a different posture (goal reach is verified by rollout afterwards). Goal safety means the generated rollout still reaches the left-wrist task-space goal; it does not mean the original plan must receive zero preference cost. If goal_conflict=true, you may set the bound to gently exclude the goal posture, but never by more than the chosen trajectory demonstrates.
- Keep magnitudes modest: this preference is ADDED to a goal-reaching objective that must stay dominant.

Return ONLY a JSON object, no prose, with these keys:
- "terms": a list (usually length 1) of objects, each:
  - "feature": one of the five feature names above (string).
  - "bound_type": "lower_bound" | "upper_bound" | "band" (string).
  - "values": for a CONSTANT lower_bound/upper_bound {"threshold": float}; for band {"low": float, "high": float}; for a POSE-DEPENDENT lower_bound/upper_bound {"cond_feature": <one of the five feature names, the joint the threshold depends on>, "points": [[cond, threshold], ...]} with at least two (conditioning-value, threshold) anchor points describing how the bound slides — the author interpolates between anchors and holds the end values outside their range, so the anchors need not be collinear. Radians.
  - "source": which trajectory + statistic each number came from, and the margin you added (string).
- "goal_safety_check": one sentence confirming each bound is satisfied by the chosen trajectory's end and explaining numerically why the chosen correction receives less preference cost than the reference original plan (string).

Full numeric summaries:
{summaries}
