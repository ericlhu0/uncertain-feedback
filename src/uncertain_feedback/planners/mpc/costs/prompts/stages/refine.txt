You are refining a care-preference cost function against rollout feedback. The preference has already been interpreted (below, fixed — do NOT re-interpret it). Your job across this conversation is to ground that preference in numbers AND implement it as one small, vectorized, joint-space Python cost, then improve it as you see how it actually steers the arm. Both the numeric choices (which joint feature, bound type, thresholds) AND the code are yours to change each round; only the interpreted preference stays fixed.

Interpreted preference (fixed):
{interpretation}

Available joint-angle features (radians; specify the preference over these):
- elbow_flexion: how bent the elbow is (larger = more bent; 0 = arm fully straight, π/2 = bent to a right angle, π = forearm folded back against the upper arm).
- shoulder_flexion_extension: signed depth of the upper arm (0 = neither forward nor back, +π/2 = pointing straight forward, −π/2 = straight back).
- shoulder_abduction_adduction: signed lateral position (0 = neither out nor across, +π/2 = straight out to the side away from the torso for the left arm, −π/2 = fully across the body).
- shoulder_elevation: total upper-arm elevation from straight down (0 = arm at side, π/2 = horizontal, π = overhead); increases as the arm is raised in any direction.
- shoulder_internal_external_rotation: signed twist of the upper arm about its long axis (0 = neutral/untwisted, positive = internal rotation twisting the forearm toward the body midline, negative = external rotation twisting it away).
Each appears in the summaries under "joint_features" for every trajectory, with min / median / max / start / end.

How to ground and implement:
- Choose the FEWEST features that express the preference — default to exactly one.
- Use a one-sided bound (penalize going below/above a threshold) or a band ([low, high]); avoid exact targets unless the preference clearly demands a precise posture. Penalize only the future timesteps and reduce to one scalar per rollout.
- A one-sided bound may also be POSE-DEPENDENT: let its threshold shift with the current value of another feature (e.g. require more elbow flexion the higher the arm is raised), computed per timestep by interpolating the threshold from a few anchor points (it need not be linear). Do this ONLY when the feedback clearly ties one joint's acceptable range to another joint's position; otherwise use a constant threshold.
- Draw the numbers from the summaries; a value on the chosen trajectory is an EXAMPLE inside a comfortable set, so add margin rather than copying it exactly.
- The resulting total cost MUST score the chosen correction strictly lower than the "reference" original plan and each marked-wrong candidate. Do not use a one-sided bound that gives these trajectories the same penalty. If the chosen value lies between the reference and marked-wrong values, use a band when needed to prefer the chosen motion to all of them.
- Stay goal-safe: every bound must already be satisfied by the chosen trajectory's end — but NOT necessarily by the "reference" (original-goal) end, which is the posture the person corrected away from. Goal safety means the generated rollout still reaches the left-wrist task-space goal; it does not mean the original plan must receive zero preference cost. The arm can usually reach the same goal in a different posture, and the rollout feedback will tell you if your cost blocks the goal. Keep the magnitude modest so the goal-reaching term stays dominant.

When you receive feedback (how your cost ranks the user's chosen correction against the original plan and the marked-wrong candidates, plus a joint-feature comparison and images when available), read which preference ordering is violated or which joint is off, then adjust the threshold, bound type, feature, or the penalty magnitude to fix it on the next attempt. Your cost does not need to reproduce the chosen trajectory — it needs to prefer it.

Full numeric summaries:
{summaries}
